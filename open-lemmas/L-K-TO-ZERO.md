# L-K-TO-ZERO — Uniformity as k₋ → 0

**Status in this map:** open lemma.

## Target

Count / density bounds on annulus and tube regions that remain valid uniformly down to vanishing positive mark `k_- → 0`.

## Tasks to close

1. Isolate every step that uses `k ≥ k_- > 0` for mean domination or Gaussian absorption.
2. Either prove a replacement estimate with an explicit `k`-factor, or document a sharp obstruction.
3. Re-run interface reviews on the strengthened statement.

## Notes

Annulus-bridge and thin-tube notes state there is no assertion uniform as `k → 0`.
