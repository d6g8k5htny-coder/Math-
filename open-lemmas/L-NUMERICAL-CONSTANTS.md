# L-NUMERICAL-CONSTANTS — Effective constants and C_H

**Status in this map:** open lemma.

## Target

Numerical enclosures for existence constants `C`, `c`, `r_*` appearing in continuum accepts, and FREEZE-grade `C_H` on the historical RN route.

## Tasks to close

1. For each accepted existence bound that will be used downstream, produce a reproducible enclosure or an explicit refusal.
2. On the historical route, certify `C_H` only after ENV-RESCOV / absent-carrier recovery ([L-RN-UNIF](L-RN-UNIF.md)).
3. Separate “enough digits for a grid check” from “certificate of the continuum constant.”

## Notes

Nonauthor ACCEPT of qualitative `C`, `r_*` is not a numerical enclosure.
