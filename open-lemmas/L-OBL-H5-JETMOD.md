# L-OBL-H5-JETMOD — Jet-mod Schur / joint cancel

**Status in this map:** open lemma.

## Target

OBL-H5-JETMOD: joint `(r,y)` cancelled detgg / interval Schur bridge needed by the historical RN numerical program.

## Tasks to close

1. Produce a joint `(r,y)` cancel rewrite that does not straddle under positive-width interval arithmetic (current interval-Schur-via-Ainv: REFUSED_IA_STRADDLES).
2. Supply `explicit_interval_map_F_G12box_to_Rplus` (eval_F sibling currently REFUSED).
3. Locate or derive `correlated_C_Ainv_Ct_cancellation_under_joint_r_y` / StationBox cancel formula (hunt CLOSED EMPTY).
4. Restore or replace the `φ(det A) → detgg` bridge (ABSENT).

## Notes

Inventable probe receipts are honesty labels only; they do not discharge this obligation.
